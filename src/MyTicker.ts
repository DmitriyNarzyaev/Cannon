import {Main} from "./Main";
import Sprite = PIXI.Sprite;
import {Mob} from "./Mob";
import Global from "./Global";
import MainContainer from "./MainContainer";

export class MyTicker
{
	private static readonly shotSpeed:number = 14;
	//public static SCORE:number = 0;
	// private static shotRadius:number = 10;
	// private static xdiff:number;
	// private static ydiff:number;
	// private static distance:number;



	

		//Player.shotPack.forEach((shot) => {
			// shot.x += (Math.PI / 2 + shot.gunRotationSave)*Collision.shotSpeed;
			// shot.y += (shot.gunRotationSave)*Collision.shotSpeed;
			// shot.shotSpeedY += shot.shotGravity;
			// shot.y += shot.shotSpeedY;
		//});
	

    

	// public static stop():void {
	// 	Global.PIXI_APP.ticker.remove(MyTicker.tick);
	// }
}